.model small
.stack 100h

.data
four dW 4

CLASS DB 'MARY ALLEN  ','1','1','2','2'
      DB 'SCOTT BAYLIS',7,5,8,4
      DB 'GEORGE FRANK',8,2,9,0
      DB 'SAM WONG    ',8,6,2,6

A DB ?
.CODE

MAIN PROC

MOV AX,@DATA
MOV DS,AX


MOV A,0
XOR BX,BX

REPEAT:
 MOV CX,4
MOV SI,0
XOR AX,AX

TOP:

CMP SI,11
JG FOR1

MOV AH,2
MOV DL,CLASS[BX][SI]
INT 21H
INC SI
JMP TOP

FOR1:
MOV AH,2
MOV DL,':'

INT 21H
XOR AX,AX

FOR:

XOR DX,DX
MOV DL,CLASS[BX][SI]

ADD AX,DX
INC SI
LOOP FOR





XOR DX,DX

DIV FOUR

CALL OUTDEC
MOV AH,2
MOV DL,0AH
INT 21H
MOV DL,0DH
INT 21H

ADD BX,16
INC A
CMP A,4
JL REPEAT 


MOV AH,4CH
INT 21H

MAIN ENDP

INCLUDE OUTDEC.ASM

END MAIN
















