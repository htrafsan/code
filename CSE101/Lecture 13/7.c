#include<stdio.h>

int main() {
    int i,j,n;
    scanf("%d",&n);
    int a[n];
    for (i=0;i<n;i+=1) {
        scanf("%d",&a[i]);
    }
    /// sum of all numbers in the array
    /// maximum of all numbers in the array
    /// minimum of all numbers in the array
    /// average of all numbers in the array
    return 0;
}
